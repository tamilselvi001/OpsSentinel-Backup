"""Arize Phoenix MCP server — exposes accuracy / calibration / novelty / outcome-logging tools.

Returns raw observability metrics only. Mapping metrics -> autonomy tier is the Agent Layer's
loop (Phase 3); this server just exposes faithful data.
"""
